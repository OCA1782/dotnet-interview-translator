<Window x:Class="InterviewTranslator.Desktop.AnswerCardsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cevap Kartları"
        Width="620" Height="660"
        WindowStartupLocation="Manual"
        Topmost="True" ShowInTaskbar="False"
        Background="{StaticResource SurfaceBrush}"
        Loaded="Window_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>   <!-- Başlık -->
            <RowDefinition Height="Auto"/> <!-- Arama -->
            <RowDefinition Height="Auto"/> <!-- Kategori tab'ları -->
            <RowDefinition Height="*"/>    <!-- Liste -->
            <RowDefinition Height="Auto"/> <!-- Önizleme + Kopyala -->
        </Grid.RowDefinitions>

        <!-- Başlık çubuğu -->
        <Border Grid.Row="0" Background="{StaticResource DarkBrush}">
            <Grid Margin="16,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📋" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="Hazır Cevap Kartları" FontSize="14" FontWeight="SemiBold"
                                   Foreground="White"/>
                        <TextBlock x:Name="StatusLabel"
                                   Text="Yerel veriler"
                                   FontSize="10" Foreground="{StaticResource TextMutedBrush}"/>
                    </StackPanel>
                </StackPanel>
                <Button x:Name="RefreshBtn"
                        Content="↻ GitHub'dan Yenile"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Height="28" Padding="10,0"
                        FontSize="11"
                        Style="{StaticResource PrimaryButton}"
                        Click="RefreshBtn_Click"/>
            </Grid>
        </Border>

        <!-- Arama -->
        <Border Grid.Row="1" Background="White" Padding="14,10"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox"
                         TextChanged="SearchBox_TextChanged"
                         FontSize="13" Padding="10,8"
                         BorderBrush="{StaticResource BorderBrush}"
                         BorderThickness="1">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border CornerRadius="7"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Background="{StaticResource SurfaceBrush}"
                                                Padding="{TemplateBinding Padding}">
                                            <ScrollViewer x:Name="PART_ContentHost"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBlock Text="🔍  Konu veya cevap ara..." IsHitTestVisible="False"
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="13" Padding="14,8,0,0"
                           VerticalAlignment="Top">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>

        <!-- Kategori tab'ları -->
        <Border Grid.Row="2" Background="White"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <TabControl x:Name="CategoryTabs"
                        SelectionChanged="CategoryTabs_SelectionChanged"
                        Background="Transparent" BorderThickness="0" Padding="0">
                <TabControl.Template>
                    <ControlTemplate TargetType="TabControl">
                        <TabPanel IsItemsHost="True" Background="White"/>
                    </ControlTemplate>
                </TabControl.Template>
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Cursor"   Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="Bd" Padding="16,10"
                                            BorderThickness="0,0,0,2"
                                            BorderBrush="Transparent"
                                            Background="Transparent">
                                        <ContentPresenter ContentSource="Header"
                                                          HorizontalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="#2563EB"/>
                                            <Setter Property="Foreground" Value="#1E40AF"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                                <Condition Property="IsSelected"  Value="False"/>
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Bd" Property="Background" Value="#F8FAFC"/>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabItem Header="Tümü"       Tag="all"       Foreground="{StaticResource TextSecondaryBrush}"/>
                <TabItem Header="Teknik"     Tag="technical" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TabItem Header="Behavioral" Tag="behavioral" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TabItem Header="Genel"      Tag="general"   Foreground="{StaticResource TextSecondaryBrush}"/>
            </TabControl>
        </Border>

        <!-- Kart listesi -->
        <ListView x:Name="CardList" Grid.Row="3"
                  SelectionChanged="CardList_SelectionChanged"
                  MouseDoubleClick="CardList_DoubleClick"
                  Background="White" BorderThickness="0"
                  FontSize="13">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Konu" Width="140"
                                    DisplayMemberBinding="{Binding Topic}"/>
                    <GridViewColumn Header="İngilizce Cevap" Width="420"
                                    DisplayMemberBinding="{Binding Answer}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Önizleme + Kopyala -->
        <Border Grid.Row="4" Background="White" Padding="14,12"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" CornerRadius="7" Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <TextBox x:Name="PreviewBox"
                             IsReadOnly="True" TextWrapping="Wrap"
                             Height="68" Padding="10,8" FontSize="12.5"
                             Background="Transparent" BorderThickness="0"
                             VerticalScrollBarVisibility="Auto"/>
                </Border>
                <Button Grid.Column="1" Content="📋  Kopyala"
                        Width="96" Margin="10,0,0,0"
                        VerticalAlignment="Top" Height="36"
                        Style="{StaticResource PrimaryButton}"
                        Click="CopyBtn_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
